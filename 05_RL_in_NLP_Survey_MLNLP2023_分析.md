# 05 技术分析：RL 在 NLP 中的综述（MLNLP 2023）

**论文**：Reinforcement Learning in Natural Language Processing: A Survey
**作者**：Yingli Shen、Xiaobing Zhao（中央民族大学；国家语言资源监测与研究少数民族语言中心）
**来源**：MLNLP 2023, pages 84–90, ACM（7 页）

**分析范围说明**：按既定要求，本篇为综述，**只提取与「文本分类」或「数据筛选」相关的 RL 方法**，忽略其余任务（机器翻译、对话系统、文本生成等）。同时保留综述自身对 RL 机制的形式化定义，以支撑变量映射。

**证据层级声明**：综述对所引工作的描述为**二手转述**，本报告在引用这些工作时**标注为综述级描述**，不臆测原文细节。

---

## 结论验证

- 一句话：这是一篇**RL 在 NLP 中的应用综述**——先梳理 RL 基本原理与三大算法族，再按 NLP 任务分类回顾已有工作，最后给出未来方向与挑战；**与本项目相关的仅是其中的「文本分类」与「数据筛选」两小节**。

---

## 板块一：核心技术与 RL 机制拆解

### 1. 解决的痛点

- RL 在 NLP 中的**有效应用仍是开放问题**：缺乏统一梳理，**任务形式化方式差异大**，落地路径不清晰。
- 综述针对的缺口：**把"如何把一个 NLP 任务表述成 RL 问题"这件事系统化**。
- **综述给出的关键判据（重要）**：在本文覆盖的全部 NLP 任务中，**"none of the NLP tasks scrutinized in this study employ RL methods as their state-of-the-art methodologies"**——即 **RL 尚未成为任何被考察 NLP 任务的最先进方法**。

### 2. 综述给出的通用 RL 形式化

**MDP 四元组** $\langle S, A, T, R \rangle$：

- $S$：状态集合
- $A$：动作集合
- $T: S \times A \rightarrow \Delta(S)$：转移概率，$P(s' \mid s, a)$
- $R: S \times A \rightarrow \mathbb{R}$：奖励函数，把状态或状态—动作对映射为一个实数 $r$

**优化目标**：寻找最优策略

$$\pi_\theta = P(A=a \mid S=s)$$

以最大化**期望折扣累计回报**。

**【通俗理解】**

- 类比**"做一个反复试错的决策者"**：**看当前局面（S）→ 选一个动作（A）→ 局面变了（T）→ 收到分数（R）**。
- **策略 $\pi_\theta$ 就是那个"看到什么局面就出什么招"的决策手册**。
- 综述的价值在于：**它把"一句话该归到哪一类"这类语言任务，硬套进这个决策框架**——**这一步转化是最难、也最容易被做歪的地方**。

### 3. 三大算法族（综述的分类）

| 族 | 代表算法 | 优化对象 | 特点 |
|---|---|---|---|
| **Value-Based（值方法）** | **Q-learning**、**DQN** | **值函数 $Q(s,a)$**，不显式存策略 | 通过最优值函数**隐式**导出策略 |
| **Policy-Based（策略方法）** | **REINFORCE**、Policy Gradient | **策略参数 $\pi_\theta$** | **直接优化目标量**，函数逼近下更稳定；**样本效率低、方差大** |
| **Actor-Critic（演员—评论家）** | — | **Actor + Critic 双网络** | **策略梯度的 TD 版本**；用值近似替代 rollout 估计，**以降偏差换方差** |

**Q-learning 更新式（综述式 1）**：

$$Q(s,a) \leftarrow Q(s,a) + \alpha\left[r + \gamma \max_{a'} Q(s',a') - Q(s,a)\right]$$

**策略梯度（综述式 2）**：

$$\nabla J(\theta) = \mathbb{E}_{a \sim \pi_\theta,\ s \sim \mu_{\pi_\theta}}\left[Q^{\pi_\theta}(s,a)\,\nabla \log \pi_\theta(a \mid s)\right]$$

**【通俗理解】**

- 类比**三种学习方法**：
  - **值方法（Q-learning）＝记分表**：**只记"每种局面下每个动作值多少分"**，出招时**挑分最高的**——**不写策略，策略从表里读出来**。
  - **策略方法（REINFORCE）＝直接练手感**：**不管每步值多少分，直接把"刚才那套动作"的倾向调强或调弱**——**简单但手抖（方差大）**。
  - **演员—评论家＝选手配教练**：**选手出招（Actor），教练当场评分并指导（Critic）**——**比纯靠赛后总分的练法更稳**。

### 4. 与「文本分类」相关的 RL 方法（综述转述，共 3 项）

| 工作 | 出处 | RL 的用途（综述级描述） |
|---|---|---|
| **Zhang et al.** | AAAI 2018（*Learning Structured Representation for Text Classification via RL*） | 用 RL **发现与任务相关的结构**，构建**结构化的句子表示**用于文本分类 |
| **Mao et al.** | EMNLP 2019（*Hierarchical Text Classification with Reinforced Label Assignment*） | 把**层次文本分类（HTC）形式化为 MDP**，用**深度 RL 学习标签分配策略**，决定**把对象放到哪一层**以及**何时停止分配** |
| **Ye et al.** | ACL 2020（*Zero-shot Text Classification via Reinforced Self-training*） | **RL 框架自动学习数据选择策略**，以实现**更可靠的选择**（零样本场景） |

**【通俗理解】**

- **Zhang 2018 ≈ 先搭骨架再分类**：**让模型自己决定"这句话该按什么结构来读"**（比如先找主语再找谓语），**而不是把整句揉成一个向量**。
- **Mao 2019 ≈ 归类时先决定"放哪个抽屉、什么时候停"**：**深度 RL 学的是"把这篇文章放进分类树的哪一层"以及"什么时候不再往下分"**——**动作＝标签分配，终止＝停止分配**。
- **Ye 2020 ≈ "自己挑训练样本"**：**让 RL 学会从一堆生成/伪标注数据里挑出可靠的那些**——**这正是"数据筛选"类方法**。

### 5. 与「数据筛选 / 噪声过滤」相关的 RL 方法（综述转述）

| 工作 | 出处 | RL 的用途（综述级描述） |
|---|---|---|
| **Ye et al.** | ACL 2020 | **RL 自动学习数据选择策略**（零样本文本分类）——**直接对应"数据筛选"** |
| **Hu et al.** | EMNLP 2021（*Gradient Imitation RL for Low Resource Relation Extraction*） | **让伪标注数据模仿标注数据的梯度下降方向**，通过试错提升优化能力——**本质是伪标签的筛选与对齐** |
| **Xiao et al.** | AAAI 2020（Transformer + RL 实体关系抽取） | RL 模块用于关系分类，**并能过滤掉含噪句子** |

**【通俗理解】**

- 共同套路：**"用 RL 决定'哪些数据值得用'"**。
- **Ye 2020**：类比**"雇一个人专门挑靠谱的练习题"**——**题目质量参差时，挑题能力本身就是可学习的技能**。
- **Hu 2021**：类比**"让自学笔记往'标准答案的解题方向'靠"**——**伪标签不直接当答案用，而是用它指个方向**。
- **Xiao 2020**：类比**"边做题边把明显胡扯的题扔掉"**——**过滤噪声是 RL 模块的附带收益**。

### 6. 综述给出的「自然语言理解即 MDP」形式化（对分类最有参考价值的一段）

- 原文表述：**NLU 可表述为 MDP**，其中
  - **State ＝ 当前对文本的理解**
  - **Action ＝ 更新该理解的可能方式**
  - **Reward ＝ 更新后的理解与文本真实含义的匹配程度**

**【通俗理解】**

- 这套映射最贴近分类任务：**"理解"可以被逐步更新**，而不是一次读完就定型。
- 类比**读一篇论文时的渐进判断**：**先看标题形成初判（state）→ 翻摘要修正（action）→ 看结论再修正（action）**，**每一步都用"是否更接近真实类别"来打分（reward）**。

### 关键变量映射

> 说明：本表综合**综述的通用形式化**与**其所引 3 项文本分类工作**的描述。综述本身**未给出这些工作的具体公式**，故符号为**按标准 MDP 形式化标注**，非原文符号。

| 变量 | 数学符号 | 物理含义 | 在语义空间中的操作 |
|---|---|---|---|
| State | $s \in S$ | **综述通用**：环境当前局面；**分类场景**：当前对文本的理解 / 已分配的标签结构 / 待选数据池的状态 | 在文本表示或标签结构上表示"当前进展到哪" |
| Action | $a \in A$ | **综述通用**：可选动作；**分类场景**：**选择结构、分配标签、判定是否终止、挑选样本** | 对文本表示结构、标签树位置或数据池执行离散操作 |
| Transition | $T: S\times A \to \Delta(S)$，$P(s'\|s,a)$ | 执行动作后的状态转移 | 理解状态或标签分配状态被更新 |
| Reward | $R: S\times A \to \mathbb{R}$ | **综述通用**：实数奖励；**分类场景**：**分类正确性 / 理解匹配度 / 所选数据的可靠性** | 作用于**类别标签**或**理解结果**的比对，非词向量层面 |
| Policy | $\pi_\theta(a\|s)$ | 决策策略 | 由文本表示经参数化网络输出动作分布 |
| 值函数 | $Q(s,a)$ | 状态—动作对的价值 | 对"在当前理解下执行某动作"的长期收益估计 |
| 折扣因子 | $\gamma$ | 未来奖励权重 | — |
| 学习率 | $\alpha$ | 更新步长 | — |

**【通俗理解】**

- 整张表的物理图景：**"把'读一句话并分类'拆成一串小决定，每个小决定都给分"**。
- **State 是"我现在读到哪、理解到哪"，Action 是"下一步怎么处理"，Reward 是"处理完离正确答案更近了吗"**。
- **最值得注意的是 Reward 的位置**：它**落在标签/理解层面**，**不在词向量层面**——**这意味着分类任务的 RL 奖励天然是离散、低信息量的**。

---

## 板块二：算法实现细节

### 1. RL 算法选型

- 本篇为综述，**不提出自己的算法**；其贡献是**建立选型分类框架**。
- **综述给出的选型逻辑（由其分类可推出）**：

| 场景特征 | 适配算法族 | 依据（综述原文要点） |
|---|---|---|
| 动作空间小、需精确价值估计 | **值方法（Q-learning / DQN）** | 隐式导出策略，实现简单 |
| 需直接优化目标量、动作连续或巨大 | **策略方法（REINFORCE / PG）** | 可直接优化关注量，**函数逼近下更稳定** |
| 需降低方差且可承受偏差 | **Actor-Critic** | **TD 版策略梯度**，用值近似替代 rollout 估计 |

- **原文对各族的批评（关键技术判断）**：
  - 策略梯度类**样本效率低**，因梯度由 rollout 估计，**导致高方差与次优性能**。
  - 值方法**理论基础仍不充分**：DQN 虽实践成功，但**"深 Q-learning 为何能达到近优目标值"仍缺乏理论理解**。

**【通俗理解】**

- 类比**选工具的三种思路**：**局面少就查表（值方法）；局面复杂就练手感（策略方法）；想要稳就配教练（Actor-Critic）**。
- 综述最有价值的一句话是**"RL 还没成为任何 NLP 任务的最强方法"**——**说明这条路目前仍是"有潜力但未兑现"**，**不是现成的银弹**。

### 2. 训练流程

- 综述**未统一规定训练流程**；但从所回顾的分类工作可归纳出**三种典型范式**：

| 范式 | 代表 | 流程特征 |
|---|---|---|
| **RL 作为表示学习器** | Zhang et al. 2018 | **RL 负责"怎么读"**（发现结构），**分类器负责"怎么判"**——**两段式** |
| **RL 作为决策器（序列决策）** | Mao et al. 2019 | **把标签分配本身建模为 MDP**，**逐层决定放哪、何时停**——**端到端序列决策** |
| **RL 作为数据选择器** | Ye et al. 2020；Hu et al. 2021 | **RL 负责挑样本或对齐伪标签**，下游仍是常规监督训练——**数据侧增强** |

**【通俗理解】**

- 三条路线的分工很清楚：
  - **第一条：RL 管"怎么看"**（结构化阅读方式）
  - **第二条：RL 管"怎么归类"**（分类动作本身）
  - **第三条：RL 管"用哪些材料"**（数据质量）
- **与分类任务最贴合的是第二条与第三条**：**前者把分类当决策过程，后者把训练数据当可优化对象**。

### 3. 损失函数设计

- 综述**未给出统一的损失函数形式**，但在原理部分给出了两族的**基本优化目标**：

**值方法（时序差分目标）**：

$$Q(s,a) \leftarrow Q(s,a) + \alpha\left[r + \gamma \max_{a'} Q(s',a') - Q(s,a)\right]$$

**策略方法（策略梯度）**：

$$\nabla J(\theta) = \mathbb{E}\left[Q^{\pi_\theta}(s,a)\,\nabla \log \pi_\theta(a \mid s)\right]$$

- **是否结合交叉熵（SFT）**：**综述层面未讨论**该组合（**属于本篇之后才成为主流的设计**）。
- **是否使用 KL 散度**：**综述未提及**。
- **方差控制手段（原文）**：**Actor-Critic 用值近似替代 rollout 估计以降低方差**，代价是引入偏差。

**【通俗理解】**

- **值方法的目标就是"把表里的分数修准"**：**新结果 + 老判断的差值，按比例改**。
- **策略方法的目标是"把手感调准"**：**得分高的动作，就提高它被选中的概率**。
- **值得注意两个"没有"**：**没有交叉熵、没有 KL**——**说明 2023 年这份综述成稿时，"SFT + RL 混合训练"尚未成为范式**；而这恰是 2025 年 GTA（第 04 篇）的核心贡献。**两份文献的时间差本身就说明了领域演进方向。**

---

## 板块三：技术局限与难点

### 1. 算力与数据瓶颈

- **综述未给出具体的算力数字**（作为综述，其粒度是方法层面而非工程层面）。
- **可推断的硬性要求**：
  - **策略方法需要大量 rollout** → **样本效率低**，**训练成本高**。
  - **Actor-Critic 需要额外的 Critic 网络** → **显存与计算量翻倍**。
- **数据侧的普遍困难**：NLP 任务中**奖励通常在生成完整序列后才能获得**，**标注与反馈成本高**。

**【通俗理解】**

- 类比**"练习次数不够就学不会"**：**策略方法要靠反复试错积累经验**，**试错次数本身就是成本**。
- **"配教练"要额外开销**：**Actor-Critic 多养一个网络，显存和时间都要多花一份**。
- **奖励来得太晚**：**写完一整篇才有反馈**，**中间哪一步做错了不好定位**。

### 2. 技术坑点（综述列出的三类挑战）

**挑战一：鲁棒性问题（Robustness）**

- RL 算法对**随机种子、超参数、奖励函数、运行环境**均敏感。
- 需要智能体**保留每个具体任务的身份**，以按环境选择对应策略。
- **Robust RL 的目标**：**在多种可能环境下都有效**，而非只针对单一理想环境调优。

**挑战二：安全性问题（Safety）**

- 智能体处于**动态且不确定**的环境，**状态转移与奖励分布可能随时间或情境变化**。
- 受**噪声、带宽、隐私、语言障碍**等因素影响，**策略更新与信念更新可能失效**。
- **环境本身会被智能体行为改变**，从而**催生欺骗、操纵、错误信息等新风险**。

**挑战三：反馈与信用分配问题（Feedback / Credit Assignment）**

- RL 依赖环境反馈，但 NLP 任务中**智能体往往要生成很长一段序列才能获得奖励**。
- **核心困难**：**智能体难以把收到的奖励归因到序列的具体部分**，**从而难以从错误中有效学习**。

**【通俗理解】**

- **鲁棒性问题**：类比**"同一道菜换个厨房就做不出来"**——**换个随机种子或超参，结果就变**。
- **安全问题**：类比**"环境会因为你的动作而改变，还可能反噬你"**——**比如模型学会钻奖励空子，产出误导性内容**。
- **信用分配问题（最要命的一条）**：类比**"一篇作文只给一个总分，不告诉你哪句写得好"**——**模型知道"总体不行"，但不知道"该改哪一句"**。
  - **这一条对分类任务尤其致命**：**分类输出通常很短**，**但奖励信号更稀疏**（往往就是"对/错"二值）。

### 3. 综述自述的方向（Visionary Prospects，节选与分类相关者）

- **信息抽取方向**：**分层 RL** 用**高层策略选择最相关的句子/段落**，**低层策略从选中文本中抽取实体关系**；**高层策略还能学会"没有更多有用信息时终止"**——**层级化"选择 + 终止"结构**。
- **NLPO（自然语言策略优化）**：**用全局奖励函数评估整条生成序列**，而非**逐 token 的局部奖励**，以**捕捉长期依赖、保证全局连贯**。

**【通俗理解】**

- **分层 RL 的"高层选择 + 低层抽取 + 何时停"**：类比**"先挑出书上哪几页值得看，再在选中的页面里找答案，找完就合书"**——**这套结构在结构上更接近"筛选 + 决策"的任务形态**。
- **NLPO 的全局奖励**：类比**"按整篇作文打分，而不是按每个词打分"**——**避免"逐词都合格但整体走样"**。

---

## 结论部分要点

- **综述定位**：系统梳理 RL 基础方法学，并广泛回顾其在 NLP 中的应用，同时指出**潜在轨迹与重大挑战**。
- **最关键的一句判断（原文）**：
  > **"at present, none of the NLP tasks scrutinized in this study employ RL methods as their state-of-the-art methodologies."**
  - 即：**在被考察的全部 NLP 任务中，RL 均未成为最先进方法**。
- **作者意图**：**照亮 RL 在 NLP 中的研究现状与应用场景**，以**鼓励在该范式上继续探索**。

**【通俗理解】**

- 这句话等于作者自己承认：**RL 在 NLP 里"看起来很美，但还没赢过常规方法"**。
- 对本项目的含义：**把 RL 用在文本分类上，属于"探索性前沿"而非"稳妥路线"**——**收益不确定，但研究空间真实存在**。

---

## 本综述中最有价值的三个技术线索（面向分类任务）

1. **「标签分配即序列决策」范式（Mao et al. 2019）**
   - **把分类本身写成 MDP**：动作＝标签分配，含**显式终止动作**。
   - 价值：**为分类任务提供了最直接可用的 RL 形式化**，且**终止动作天然适配"层次分类"与"变长决策"**。

2. **「RL 学数据选择策略」范式（Ye et al. 2020）**
   - **把"挑哪些样本"作为可学习策略**，而非固定启发式规则。
   - 价值：**对应数据噪声与伪标签场景**，是**成本最低、最容易验证的切入点**。

3. **「分层 RL 的选择 + 终止」结构（4.1.2 节）**
   - **高层策略选段、低层策略抽取、可学习终止**。
   - 价值：**结构上通用**，可**直接映射到"先筛重要段落、再分类"的两级流程**。

**【通俗理解】**

- 三条线索可概括为三句话：**"把分类当出招"、"把挑数据当技能"、"把筛选分两级"**。
- **三者的共同点**：都是**把"选择"这件事交给 RL**——**这正是 RL 在分类任务上最自然的落点。**
