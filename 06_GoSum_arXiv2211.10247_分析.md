# 06 技术分析：GoSum——用强化学习与图组织语篇状态做长文档抽取式摘要

**论文**：GoSum: Extractive Summarization of Long Documents by Reinforcement Learning and Graph Organized discourse state
**作者**：Junyi Bian（复旦大学）、Xiaodi Huang（Charles Sturt University）、Hong Zhou（Atypon Systems）、Shanfeng Zhu（复旦大学）
**来源**：arXiv:2211.10247（10 页，**预印本**）

> **版本提示**：文件夹中为 **arXiv 预印本**；期刊正式版发表于 *Knowledge and Information Systems* 66(12): 7557–7580，**作者 5 位**（多一位 Tianyang Huang），标题中为 "Graph-Organized"。**正式版在付费墙内**，引用页码 / 作者请以期刊版为准。
> **本报告基于 arXiv 预印本原文**，共 4 位作者。

---

## 结论验证

- 一句话：提出 **GoSum**——把**抽取式摘要视为「句子分类 / 序列标注」任务**，用**异构图（句子节点 + 段落节点）+ 图注意力网络**编码句子的**语篇级状态**，再用**策略梯度（REINFORCE）逐步抽取句子**生成摘要，并配备**可学习的停止机制**。

---

## 板块一：核心技术与 RL 机制拆解

### 1. 解决的痛点

- **摘要抽取本质是"用文档结构信息做句子分类"**，但**如何利用结构信息**是难点。
- **抽象式方法的缺陷**：**易产生无意义与不忠实的内容**——**ROUGE 分数高但语义与金标摘要有明显差距**。
- **长文档的具体困难**：**序列过长**，Transformer 类编码存在**二次计算与内存复杂度**。
- **跨段落语义漂移**：长文档按段落组织（背景 / 方法 / 实验），**段落边界处语义容易漂移**。
- **前人 RL 抽取方法的不足**：传统抽取方法**固定抽取句子数量**，**无法自行决定何时停止**。

### 2. 任务形式化

- **输入**：文档 $D = \{s_1, s_2, \dots, s_n\}$（$n$ 个句子）
- **输出**：句子索引序列 $\hat{Y} = \{\hat{y}_1, \hat{y}_2, \dots, \hat{y}_T\}$，表示**哪些句子进入摘要**
- **监督信号构造**：数据集仅提供"文档—摘要"对，**无句子级标注** → 用**集束搜索（beam search）+ 自动指标 ROUGE** 采样出一组**oracle 标签** $\{Y^1, Y^2, \dots\}$，**取其间 ROUGE 分数作为 RL 奖励**。

**【通俗理解】**

- 类比**给一篇长论文"划重点"**：**先由机器划出若干个"参考答案版本"，谁划得更接近真摘要，谁就是好样本**。
- **难点在于没有标准答案告诉你"每一句该不该留"**——**所以先用 ROUGE 反推出一批近似正确答案**，**再拿它们反过来训练模型**。
- 这一步是**自举（bootstrapping）**：**用现有指标造标签，再用这些标签学策略**。

### 3. RL 与语义/文本的交互机制

**1. 文本/语义向量的编码方式**

**三步式状态编码（原文 Figure 1 / §3.3）**，三个子编码器拼成状态：

| 子编码器 | 技术构成 | 作用 |
|---|---|---|
| **① 图式语篇感知编码器**（Graph-based Discourse Awareness） | **异构图 + GAT（图注意力网络）** | 编码文档**层级结构（语篇层级）** |
| **② 全局上下文编码器**（Global Context Encoder） | — | 编码句子在**整篇文档中的全局上下文** |
| **③ 抽取历史编码器**（Extraction History Encoder, EHE） | **LSTM** | 编码**已抽取历史**，逐步更新 |

**图构造细节（原文）**：

- 节点**只有两类**：**句子节点**与**段落节点**（**刻意丢弃词节点**）。
  - 丢弃词节点的理由（原文）：**RL 训练耗时，去掉词节点可显著提升运行速度**；且词到句的信息本质上只是句子局部内容表示，**用简单编码器（如 LSTM）已足够**。
- 边：**每个句子与其所属段落相连**；**每个段落内部构成全连接子图**。
- **图的语义作用（原文）**：边**反映文档的语篇层级，用于抑制跨段落边界的语义漂移**。

**节点初始化（原方式 4–5）**：

$$h^0_c = \text{MHP}(\text{LSTM}(\text{GloVe}(cw_1, cw_2, \dots, cw_c)))$$

$$h^0_s = \text{MHP}(\text{LSTM}(\text{GloVe}(sw_1, sw_2, \dots, sw_s)))$$

- 即：**GloVe 词向量 → BiLSTM → 多头池化（MHP）**，分别得到**段落表示 $h^0_c$** 与**句子表示 $h^0_s$**。

**图注意力更新（原文式 6–8）**：

$$e_{ij} = \text{LeakyReLU}(W_a[W_q h_i ; W_k h_j]) \qquad \alpha_{ij} = \frac{\exp(e_{ij})}{\sum_{k \in N_i}\exp(e_{ik})}$$

$$h'_i = \sigma\left(\sum_{j \in N_i}\alpha_{ij} W_v h_i\right) + h_i$$

**消息传递（原文式 9–11）**：

- **句子 → 段落**：$U_{s \to c} = \text{GAT}(H^0_c, H^0_s, H^0_s)$，$H^1_c = \text{FFN}(U_{s \to c} + H^0_c)$
- **段落 → 段落**：$U_{c \to c} = \text{GAT}(H^1_c, H^1_c, H^1_c)$

**【通俗理解】**

- 类比**"先画出论文的骨架图"**：**每一句话和每一个章节都是图上的一个点，句子挂在它所属的章节上**。
- **图注意力的作用**：**让每句话知道自己"在哪个章节、跟哪些句子是邻居"**——**而不是只按前后顺序读**。
- **为什么不建词节点**：类比**"为了跑得快，先把细枝末节砍掉"**——**RL 训练很慢，词级节点会让图变得臃肿**。
- **全局上下文编码器**＝**"总览整篇"**；**抽取历史编码器**＝**"记住已经挑过哪几句"**，避免重复。
- **状态 = 局部结构 + 全局视角 + 历史记忆**，三者拼起来。

**2. State 从语义向量中的提取方式**

- **状态是三个子表示的组合**（原文明确列举）：
  1. **句子的局部表示（含语篇感知）** —— 来自图编码器
  2. **句子在文档内的全局上下文** —— 来自全局上下文编码器
  3. **抽取历史信息** —— 来自 EHE
- 状态**不是单一向量**，而是**多源特征融合**；
- 图编码部分**经 GAT 的消息传递**得到，**不是简单拼接或池化**；**初始化用的是 GloVe + BiLSTM + MHP**。

**【通俗理解】**

- 类比**做判断题时的三种依据**：**① 这句在文中什么位置（结构）② 全文在讲什么（全局）③ 我已经选过哪几句（历史）**。
- 关键：**三者都要看，缺一不可**——**只看单句会漏掉上下文，不看历史会重复选**。

**3. Action 作用于文本的方式**

- **动作定义（原文明确）**：**"对句子评分并选择"——即抽取一个句子**（`extract a sentence`）。
- **动作 = 选择句子索引**，是**离散选择动作**，**不生成新文本、不修改句子内容**。
- **额外设置一个终止动作（STOP）**：模型自行决定**何时停止抽取**。

$$p_{stop} = p(\text{stop} \mid S_t, \theta)$$

- **动作执行规则（原文）**：**每步策略网络先输出 $p_{stop}$**；**若大于预设阈值则停止抽取，否则继续寻找下一个句子**。

**【通俗理解】**

- 类比**"边读边做标记"**：**每读一句就决定"要不要画线"**——**动作就是"画"或"不画"**。
- **STOP 动作相当于"我选够了，收工"**：**不再依赖"固定抽 3 句"这种硬规则**，**模型自己判断什么时候停**。
- 这一点在原文里是**明确对比前人工作的改进点**：**传统抽取式方法固定抽取数量，GoSum 用停止机制自行决定**。

**4. Reward 的计算方式**

- **奖励 = oracle 摘要与金标摘要之间的 ROUGE 分数**：

$$r = \frac{1}{3}\left(\text{ROUGE-1}_f + \text{ROUGE-2}_f + \text{ROUGE-L}_f\right) \quad (1)$$

- **奖励列表**：$R = (r_1, r_2, \dots, r_T)$，**$r_t$ 表示在已选集合 $\{y_1, \dots, y_{t-1}\}$ 之后，选择句子 $y_t$ 的奖励**。
- **优化目标**：$L(\theta) = \mathbb{E}_{\pi_\theta}(R)$，**最大化期望回报**。
- **奖励来源**：**自动指标（ROUGE）**，**不依赖人工标注的句级标签，也不调用奖励模型**。

**【通俗理解】**

- 类比**"按摘要质量给分"**：**三档 ROUGE 分数取平均**，**分数越高说明这组句子挑得越好**。
- **奖励的构造依赖"参考答案"**：**因为原始数据只给了成品摘要，没给"哪些句子该选"**，**所以先用 ROUGE 反推**。
- **代价**：**ROUGE 本身是有偏指标**——**模型会朝"ROUGE 高分"而不是"人看着好"的方向优化**。

### 关键变量映射

| 变量 | 数学符号 | 物理含义 | 在语义空间中的操作 |
|---|---|---|---|
| State | $S_t$；含 $h^0_s$、$h^0_c$ 等 | **当前句子状态**：局部语篇表示 + 全局上下文 + 抽取历史 | 在图结构上经 **GAT 消息传递**更新，**保留语篇层级** |
| Action | $A_t$；`extract a sentence` / `stop` | **抽取某个句子** 或 **终止抽取** | 在**句子集合**上做离散选择（句子级，非 token 级） |
| Policy | $\pi_\theta(A_t \mid S_t, \theta)$ | 动作概率 | 由状态向量经 MLP 输出动作分布 |
| 停止概率 | $p_{stop} = p(\text{stop}\mid S_t,\theta)$ | **是否终止的置信度** | 与阈值比较后决定是否结束序列 |
| 联合策略 | $\pi(A_t\|S_t,\theta) = p(\text{stop}\|S_t,\theta)\,p(A_t\|\text{stop},S_t,\theta)$ | **停止与选句的联合概率** | 在动作空间上引入**显式终止维度** |
| Reward | $r=\frac{1}{3}(\text{R-1}_f+\text{R-2}_f+\text{R-L}_f)$ | **摘要质量（ROUGE）** | 作用于**句子组合的语义覆盖度**，非向量层面 |
| 回报 | $R=(r_1,\dots,r_T)$ | 逐步奖励列表 | 每个抽取步骤共用同一最终 ROUGE 值 |
| 目标函数 | $L(\theta)=\mathbb{E}_{\pi_\theta}(R)$ | 期望回报最大化 | — |
| 图节点表示 | $h^0_s$，$h^0_c$；更新后 $h'_i$ | 句子 / 段落的**初始与更新后表示** | **GloVe → BiLSTM → MHP 初始化**，**GAT 更新** |
| 结构约束 | 句子—段落边、段落内全连接 | **语篇层级** | **抑制跨段落边界的语义漂移** |

**【通俗理解】**

- 整张表的物理图景：**"把论文画成一张'章节—句子'的图 → 每句话带上结构、全局和历史三种信息 → 每次决定划哪句或收工 → 按最终摘要质量给分"**。
- **State 是"这句话在文中的位置感"，Action 是"划它还是不划、或者收工"，Reward 是"划完的摘要像不像标准答案"**。
- **最巧妙的一行是"停止概率 × 选句概率"**：**把"什么时候停"直接做成动作的一部分**，**这是对"固定抽取句数"这一硬约束的替代方案**。

---

## 板块二：算法实现细节

### 1. RL 算法选型

- 选用 **策略梯度 / REINFORCE（Williams, 1992）**，原文小节标题即为 **"GoSum via Policy Gradient"**。
- 策略梯度形式（原文式 2）：

$$\nabla L(\theta) = -\mathbb{E}_{\pi_\theta}\left[r\sum_{t=1}^{T}\nabla_\theta \log \pi_\theta(A_t \mid S_t, \theta)\right]$$

- **形式特征（需注意的技术细节）**：
  - **整条序列共用同一个标量奖励 $r$**（最终 ROUGE），**不是每步各自不同的奖励**；
  - **无折扣因子**（$\gamma$ 未出现）；
  - **无基线 / 无 Critic**（原文未引入方差缩减机制）。
- **选型理由（原文语境）**：抽取式摘要天然是**离散选择序列决策**，**策略梯度可直接优化非可微的 ROUGE 指标**。

**【通俗理解】**

- 类比**"整段听完再给一个总分"**：**不论你选了 3 句还是 10 句，最后只有一个 ROUGE 分数**。
- **问题在于"信用分配"**：**总分高，但说不清是哪一句起的作用**——**每一句都分到同样的分**。
- **没有折扣因子**意味着**早期选句和晚期选句同等重要**；**也没有"基线"来减方差**——**所以训练会比较抖**。

### 2. 训练流程

- **RL 直接训练整个网络**，**不是"先 SFT 再 RL"的两阶段**。
- 网络包含**三个子编码器 + 抽取器**，**全部由策略梯度目标统一优化**。
- **监督信号来源**：**oracle 标签由集束搜索 + ROUGE 采样得到**（**自举式构造**），**非人工句级标注**。
- **推理时**：逐步抽取，**每步先判断 $p_{stop}$ 是否超过阈值**，超过则停止。

**【通俗理解】**

- 类比**"没有标准答案，就用'参考答案'代替"**：**先用搜索算法生成若干接近真摘要的句子组合，再用它们当作训练目标**。
- **整体是端到端一步到位**，**没有"先学基础再调策略"的分段**。
- **这带来一个隐性风险**：**训练信号的质量完全取决于 oracle 标签的质量**——**参考答案造得不好，整套训练就跑偏**。

### 3. 损失函数设计

| 组件 | 是否存在 | 说明 |
|---|---|---|
| **策略梯度损失** | **是** | REINFORCE，见式 2 |
| **交叉熵（SFT）** | **未使用** | 无监督损失项 |
| **KL 散度约束** | **未使用** | 全文未出现与参考策略的 KL 项 |
| **熵正则** | **未使用** | 未见显式熵项 |
| **值函数损失** | **未使用** | 无 Critic |

- **唯一的"稳定性"设计**在**结构层面而非损失层面**：
  - **图结构约束**（句子—段落边、段落内全连接）用于**抑制跨段落语义漂移**；
  - **停止阈值**用于**约束抽取长度**，替代"固定句数"的硬规则。

**【通俗理解】**

- 类比**"只有一条评分规则，没有额外保险"**：**没有"照标准答案抄"的监督项，也没有"别跑太远"的束缚项**。
- **稳定性靠"结构"来保证而不是靠"损失"**：**图的边把句子框在章节内**（类比**"规定每个段落的句子只能在本段落内比较"**），**停止阈值把长度框住**。
- **代价**：**因为没有 KL 约束，策略漂移完全依赖奖励信号来纠正**——**奖励有偏时，漂移会被放大**。

---

## 板块三：技术局限与难点

### 1. 算力与数据瓶颈

- **数据**：两个科学文献摘要数据集：

| 数据集 | 平均句数/文档 | 平均 token/文档 | 平均段落数 | 训练规模 |
|---|---|---|---|---|
| **PubMed** | 89 | 2730 | 6.0 | 116,937 |
| **arXiv** | **207** | **5206** | 5.5 | **202,880** |

- **文档长度是核心负担**：**arXiv 平均 207 句、5206 token**，**RL 的逐步抽取会显著放大时间开销**。
  - 原文明确提到：**"reinforcement learning is time-consuming"**，**并因此主动去掉图里的词节点以提速**。
- **评测指标**：**ROUGE-1 / ROUGE-2 / ROUGE-L 的 F1**。
- **原文未披露具体 GPU 型号与训练时长**（预印本信息有限）。

**【通俗理解】**

- 类比**"一篇文章几百句，逐句决定要不要选"**——**决策步数等于句子数**，**长文档的 RL 训练天然慢**。
- **"砍掉词节点"就是被时间逼出来的工程妥协**：**宁可牺牲一点细粒度语义，也要把训练时间压下来**。

### 2. 技术坑点

1. **信用分配（credit assignment）问题突出**
   - **整条序列共用同一个 ROUGE 奖励**，公式中 $r$ 提到求和号外，**每步分到完全相同的分数**。
   - 后果：**早期错误选择与晚期正确选择无法区分**，**梯度信号含噪**。

2. **无基线、无 Critic 导致高方差**
   - 策略梯度未引入任何方差缩减机制。
   - 后果：**训练不稳定**；**在 20 万级样本上收敛成本高**。

3. **Oracle 标签质量成为性能上限**
   - 训练信号完全来自**集束搜索 + ROUGE 采样出的 oracle 摘要**。
   - 后果：**若 oracle 集合未能覆盖真正的好摘要，模型的学习目标本身就是错的**——**"目标设定偏差"无法通过更多训练弥补**。

4. **ROUGE 作为奖励的固有偏差**
   - ROUGE 是**n-gram 重叠指标**，**与语义忠实度、可读性不完全一致**。
   - 后果：**模型会朝"ROUGE 高分"而非"人类认可"方向优化**——**典型的代理指标偏差**。

5. **强依赖外部语篇结构输入（原文自述局限）**
   - 原文结论明确指出：**"GoSum requires hierarchical discourse information about long texts as its inputs"**。
   - 后果：**对没有现成章节切分的数据集无法直接使用**；**必须先把文档切成分层结构**。

6. **结构信息增益有限（消融数据）**

| 变体 | R-1 | R-2 | R-L |
|---|---|---|---|
| **GoSum** | **49.83** | **23.56** | **45.10** |
| 去掉段落间边 | 49.72 | 23.46 | 44.99 |
| **去掉图** | 49.44 | 23.24 | 44.74 |
| 去掉段落与图 | 49.22 | 23.02 | 44.49 |

   - **量化观察**：**去掉整张图，ROUGE-1 仅下降约 0.39（49.83 → 49.44）**。
   - 即：**图结构带来的是稳定但幅度较小的增益**；**性能主贡献并非来自图，而是来自基础编码与 RL 抽取框架**。

7. **动作粒度固定为句子**
   - 原文称 GoSum **"operating on a subsentential discourse unit level"**，但实现中**动作仍以句子为单位**。
   - 后果：**无法做句内细粒度筛选**；**长句中的冗余信息无法被剔除**。

**【通俗理解】**

- **信用分配**：类比**"作文只有一个总分，不告诉你哪段写得好"**——**改进方向不明确**。
- **无基线**：类比**"只试一次就下结论"**——**运气成分大，训练不稳**。
- **Oracle 质量**：类比**"参考答案本身不完整"**——**学生再努力也只能学到参考答案的水平**。
- **ROUGE 偏差**：类比**"用'字数重合度'评判文采"**——**指标一偏，优化方向就偏**。
- **依赖结构输入**：类比**"菜谱要求先把食材按类别分好，不分好就没法做"**——**前置条件成为使用门槛**。
- **增益有限**：类比**"加装了一个精密配件，但整体性能只提升 1%"**——**工程复杂度与实际收益不成比例**。
- **粒度固定**：类比**"只能整段删，不能删句子里的废话"**。

---

## 结论部分要点

- **核心贡献（原文自述三条）**：
  1. 提出 **GoSum**——**基于图的、语篇感知的抽取式摘要模型**，可生成**简洁信息量大的摘要**；
  2. **在 GoSum 中有效整合 RL 与 GNN**——**借助 RL 获得充足样本，依靠 GNN 捕捉文档的语篇层级信息**；
  3. **在两个基准数据集（PubMed、arXiv）上取得 SOTA**，优于抽取式与抽象式强基线。
- **消融结论（原文）**：**GoSum 的性能收益来自于"句子的层级归属"与"段落标题的语义信息"**。
- **自述局限与未来方向**：
  - **局限**：**GoSum 需要长文本的分层语篇信息作为输入**。
  - **未来**：**尝试从文档自动生成语篇信息**（即去掉对外部结构标注的依赖）。

**【通俗理解】**

- **结论的核心**：**这套方法确实能打（SOTA），但前提是"论文已经分好章节"**。
- **作者自己指出的下一步**：**让机器自己认出章节结构**——**把"前置条件"这件事变成模型能力的一部分**。
- **一个需要读者自行注意的点**：**消融显示去掉图后性能只掉约 0.4 个点**，**说明"图 + 语篇"的贡献在数值上是温和的**，**而摘要质量的提升更多来自 RL 抽取框架本身**。这一点在原文结论中没有被强调，属于**从消融表中读出的隐含结论**。
